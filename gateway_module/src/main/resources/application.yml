server:
  port: 9527

spring:
  application:
    name: gateway_module
  cloud:
    consul:
      host: localhost
      port: 8500

logging:
  level:
    org.springframework.cloud: DEBUG

#zuul:
#  discovery:
#    enabled: true
#  routes:
#    records:
#      path: /records/**
#      service-id: records-module
